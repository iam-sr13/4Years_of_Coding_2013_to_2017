#include<stdio.h>
int main()
{
	float l,b,area;
	printf ("\n Enter the values for length and breadth:");
	scanf ("%g %g", &l,&b);
	area=l*b;
	printf ("\n The area of the rectangle =%g",area);
	return 0;
	}
